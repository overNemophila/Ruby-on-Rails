<nav class="navbar navbar-expand-lg navbar-light pb-3 pt-3" style="background-color: #e3f2fd;">
  <div class="container">
    <a class="navbar-brand" href="<%= root_path %>">Memberships</a>

      <div class="d-flex">
        <% if user_signed_in? %>
          <%= link_to("ログアウト", destroy_user_session_path, method: :delete, class: "btn btn-outline-primary") %>
        <% else %>
          <%= link_to("ログイン", new_user_session_path, class: "btn btn-outline-primary me-1") %>
        <% end %>
      </div>
  </div>
</nav>
