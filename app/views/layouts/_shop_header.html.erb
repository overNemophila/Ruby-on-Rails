<nav class="navbar navbar-expand-lg bg-dark navbar-dark pb-3 pt-3">
  <div class="container">
    <a class="navbar-brand" href="<%= shop_path %>"><%= @current_shop&.name || "店舗" %></a>

      <div class="d-flex">
        <% if shop_administrator_signed_in? %>
          <%= link_to("sign out", destroy_shop_administrator_session_path, method: :delete, class: "btn btn-outline-light") %>
        <% else %>
          <%= link_to("sign in", new_shop_administrator_session_path, class: "btn btn-outline-light me-1") %>
        <% end %>
      </div>
  </div>
</nav>
